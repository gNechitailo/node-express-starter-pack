const makeAuthService = require('./authService');
const makeUserService = require('./userService');
const makeMailService = require('./mailService');

module.exports = {
  makeAuthService,
  makeUserService,
  makeMailService,
};
